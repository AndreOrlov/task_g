defmodule GeoTasks.TasksController do
  use GeoTasks.Controller
  def create(_conn) do
    {:ok, %{}}
  end

  def list(_conn) do
    {:ok, []}
  end

  def pick(_conn) do
    {:ok, %{}}
  end

  def finish(_conn) do
    {:ok, %{}}
  end
end
